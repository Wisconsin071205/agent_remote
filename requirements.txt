# 运行时依赖：当前核心代码仅使用 Python 标准库，无需第三方包。
#
# 确定性工作流内核将引入（届时在此解除注释并锁定版本）：
# pymatgen>=2024.1.0      # VASP 输入输出解析、输入集生成
# custodian>=2024.1.0     # 常见 VASP 错误检测与处理规则
# py4vasp>=0.6.0          # vaspout.h5 官方结构化结果读取（需 VASP 6.2+ HDF5）
# ase>=3.22.0             # 结构格式互操作
