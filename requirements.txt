# 运行时依赖：核心代码为「可选依赖」架构——
# 无第三方包时全部工具以标准库回退运行；安装后自动走增强路径。
#
# 推荐 Python 3.11/3.12（3.14 尚无完整科学包 wheel）。本机安装命令：
#   py -3.12 -m pip install --user pymatgen custodian
# 运行工具时用 py -3.12 以启用增强解析。
#
# 已安装（2026-08-14, py -3.12）：
pymatgen>=2024.1.0      # VASP 输入输出解析、输入集生成、结构归一化
custodian>=2024.1.0     # 常见 VASP 错误检测与处理规则（custodian_detect 双引擎之一）
#
# 未安装（可选，后续引入）：
# py4vasp>=0.6.0          # vaspout.h5 官方结构化结果读取（需 VASP 6.2+ HDF5）
# ase>=3.22.0             # 结构格式互操作
# atomate2>=0.0.16        # 成熟工作流定义（长期参考，暂不整体迁移）
